\begin{alg}
  \signed{radixSort}{A, d}{A_\leq}
  \params{An array $A$ of $n$ elements, each with $d$ digits}{The array $A$ sorted in non-decreasing order}

  \For{$i \gets 1$ \KwTo $d$}{
    use a stable sort to sort array $A$ on digit $i$\;
  }
\end{alg}