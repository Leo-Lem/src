\begin{alg}
  % Algo(A, l, r)
  % n←r−l+1 ifn≤2
  % ifn=2
  % return |A[r] − A[l]|
  % else return 0 else
  % p←⌊(l+r)/2⌋
  % a ← Algo(A, l, p) b←Algo(A,p+1,r) ifa>b
  % c←a else c←b
  % fori←ltop
  % for j ← p + 1 to r
  % Lösung
  % (a)
  % Algorithmus 1
  % (Θ(1)
  % 2T (n/2) + Θ(n2)
  % return c
  % x ← |A[i] − A[j]| if x > c
  % c←x

\end{alg}